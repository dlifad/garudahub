import 'package:flutter/material.dart';

class ChantListener extends StatelessWidget {
  final Widget child;

  const ChantListener({super.key, required this.child});

  @override
  Widget build(BuildContext context) {
    return child;
  }
}